{
  "name": "hash",
  "version": "0.0.1",
  "description": "Super simple file hashing command line tool",
  "main": "bin/hash",
  "bin": {
    "hash": "bin/hash"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node_hash": "^0.2.0",
    "commander": "^2.1.0"
  }
}
